patch:
  # 修改菜单键
  switcher:
    hotkeys:
      - Control+F1
  # 默认英文
  switches/@0/reset: 1
  # 修改输入法切换键
  ascii_composer:
    good_old_caps_lock: true
    switch_key:
      Shift_L: noop
      Shift_R: commit_text
      Control_L: noop
      Control_R: commit_code
      Caps_Lock: nooop